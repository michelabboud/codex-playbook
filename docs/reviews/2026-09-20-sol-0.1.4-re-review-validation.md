# Validation of Sol's re-review of the 0.1.4 repair and source 0.1.15

**Reviewed:** `codex-playbook` target `0bfac6c` (repair base `c670327`, whole-change base
`44cdfef`); `claude-code-playbook` target `e5d218d` (base `3214fd4`). **Reviewer:** Sol
(`gpt-5.6-sol`, xhigh, separate process, kept worktree pinned at the target, cold-read note
first). **Raw report:** `2026-09-20-sol-0.1.4-re-review.md` and its cold-read note, committed
byte-identical at `d084655` before this validation. **Validator:** the planning seat (Claude
Fable 5.1), re-deriving each finding against the pinned commits.

**Verdict accepted: FAIL on both. Neither candidate is published; the line stays stopped.**
**Scoreboard: 10 findings — 9 CONFIRMED, 1 DISMISSED with a reason. Five blocking: one in the
rule (both repositories), three in the installer, one in the tests.**

| # | Severity | Verdict | What I checked, and the ruling |
|---|---|---|---|
| **1** | blocking | **CONFIRMED — the rule, fourth correction** | Rule 3.5 says "a new batch starts only while at most two closed batches are unruled" and nowhere says what *starts* means or that one batch is open at a time. Starting N+2 does not change the count of closed batches, so N+3 can be admitted on the same count and the three-range bound is exceeded by an agent obeying every word. Sol judged all twelve rows correct and the defect to be the *missing* rows — which is the table doing its job. Repair, written by this seat in all editions: **one batch is open per line at a time**; a batch starts at the first dispatch of a task the plan allocates to it; admission is re-checked when it closes; parallel batches run on separate lines, bounded by the merge rule. Sol's three omitted states become rows 13–15. **One state Sol did not name, found while writing the repair: at three, a fix must still land, or the count can never come down.** "Nothing new starts" read literally forbids the only work that rules a batch. Row 16 and one sentence: *at three, the only work the line accepts is what rules a batch.* Sol's exact-three merge ruling is taken with that exception — a merge that carries no fix waits. |
| **2** | blocking | **CONFIRMED** | `[ ! -L ]` tests the last path component only. A symlinked `references/` passes; `cp -pR` preserves the link; the installed skill resolves its roster outside the package. I had asked whether parity with the existing `SKILL.md` check made this acceptable; it does not, and the reachable path is ordinary — git stores symlinks, so a contributed change can carry one. Ruling goes further than the finding: the defect class is *a symlink anywhere inside a managed skill*, so the preflight refuses any symlink under an active skill's source directory, not only on the path to a listed resource. |
| **3** | blocking | **CONFIRMED** | `while IFS= read -r` skips a final line with no newline, while `validate_inventory` accepts that file. The older skill loops iterate a command substitution and do not have the defect; the new loop did not match the existing pattern. Repair: iterate the way the script already does. A refusal test with an unterminated inventory naming a missing file. |
| **4** | blocking | **CONFIRMED** | A listed resource's owning skill is never checked against the active inventory, so a dependency can be preflighted and never installed. Repair: the owning skill of every resource must be an active skill, refused before any backup. |
| **5** | blocking | **CONFIRMED** | One substring per row passes a row that also says the opposite. Repair: the rule's table is compared row for row, byte for byte, against a canonical copy under `tests/`. That is a change-detector by design — a rule table should not change without a second, deliberate edit — and it proves fidelity, not correctness; independent semantic review stays. |
| **6** | minor | **CONFIRMED** | Both guides still prescribed "no programme material anywhere above a reviewer's directory", the wording the rule had dropped as unsatisfiable. Both now carry the scoped wording. |
| **7** | minor | **CONFIRMED** | The parity matrix cited only a tag that does not exist yet. It now cites the immutable source commit and names the tag as following; `PLAN.md` states the order — the source is tagged and pushed before this repository is. |
| **8** | minor | **CONFIRMED** | The visual dataset's 8.1 summary kept "no product names live in the rules". Narrowed to match the roster and README. |
| **9** | minor | **DISMISSED, with the reason** | `claude-code-playbook`'s `HANDOFF.md` says 0.1.15 is complete while the commit is held. True of the snapshot Sol read, and no reader can see that snapshot: the file is published only with the version it describes, and any wording true while held would be false once published. No edit. |
| **10** | informational | **CONFIRMED** | A blank line at the end of the first review's cold-read note, introduced by my whitespace normalisation of the raw file. Removed in its own commit; the note's content is unchanged. |

**On the rejected alternative.** I asked Sol to judge my rejection of the first review's repair
("N+2 may be built but may not close") on its merits rather than take my word. It agreed, for
the reason given — a batch whose last task has landed is closed as a matter of state — and then
showed the admission rule was incomplete anyway. Both halves are recorded because the first
half alone would have read as vindication.

**Sol's "could not verify", answered.** BusyBox and BSD `grep -E` were not run; the pattern uses
POSIX extended-regular-expression constructs only, and that remains an assessment, not
evidence — logged in `BACKLOG.md`. Browser acceptance is repeated by the coordinator after the
repair, because the dataset changes again. The fresh-session load of the installed roster is
still owed and still the owner's to authorize.

**Process note.** Three independent reviews in one day, each from the other model family, each
with a cold read first; each failed the candidate; the defect class shrank each time —
contradiction, then a boundary off by one, then omitted states. Nothing was published between
the second and the third. That is the rule under review working as written, on itself.
