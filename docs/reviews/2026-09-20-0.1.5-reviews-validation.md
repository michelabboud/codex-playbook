# Validation of the two mechanical reviews of 0.1.5

**Task:** scope the rule 3.5 wording contract to rule 3.5 (`ba6125b..`), the informational
finding of Sol's fifth review. **Worker:** Sol (`gpt-5.6-sol`, hexe profile `ari-sol`), twice —
the owner asked that the family that found it fix it. **Reviewer:** Claude Sonnet (hexe profile
`builder`, the mechanical review lane), a different family from the worker, separate process,
kept worktree pinned at the target, read-only. **Raw reports:**
`2026-09-20-0.1.5-mechanical-review.md` (target `739921e`) and
`2026-09-20-0.1.5-mechanical-recheck.md` (target `7c6b8c0`), each committed byte-identical
before this validation. **Validator:** the coordinator (Claude Fable 5.1).

**Verdicts accepted: PASS WITH CHANGES, twice; no blocker at any point.**

**First review — 4 findings, 4 CONFIRMED, all fixed inside the unpublished 0.1.5.** The failure
message listed all eight sentences instead of the one at fault; the forbidden-wording message
said "rule 3.5" for a sweep that also covers rules 3.1 and 3.3; `ad-hoc batch` was satisfied by
table row 18, so the prose sentence was not pinned — the reviewer proved it, a prose-only
mutation passed 52/52; the changelog said "at the owner's word", which the repository cannot
confirm. Coordinator's own mutation after the fix: the prose phrase and "the row wins" altered,
"ceiling of two" planted in rule 3.1 — two failure lines naming exactly those three, the planted
one at `SKILL.md:14`.

**Re-check — 3 findings.**

| # | Severity | Verdict | Ruling |
|---|---|---|---|
| 1 | minor | **CONFIRMED in substance, one premise REFUTED** | The changelog named model families without citing a record. The premise that "the commit messages name no family" is wrong — `739921e`, `24b8326` and `7c6b8c0` each name the worker and the reviewer — but a changelog should not need `git log` to be believed. It now cites the review records and drops the family clauses. A wording change to a record, validated here; it owes no further review. |
| 2 | informational | **CONFIRMED — queued** | A forbidden sentence starting with `-` would be read by `grep` as an option and let the sweep pass. None of the fourteen does, and the code it replaced behaved the same. Logged in `BACKLOG.md` (`grep -F -e`). Not changed now: a change to the contract after its re-check would owe another, and an informational finding queues. |
| 3 | informational | **CONFIRMED — no action** | `awk -v` interprets backslashes in the file path; the path is a constant without one. |

**Evidence at the published tip:** `./scripts/verify.sh` — 52 rulebook checks, 340 installer
assertions, all passing; `git diff --check` clean.
