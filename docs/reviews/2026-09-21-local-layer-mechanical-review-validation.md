# Validation — mechanical review of the local layer (0.1.6, held)

- **Review:** [`2026-09-21-local-layer-mechanical-review.md`](2026-09-21-local-layer-mechanical-review.md), committed as returned. Reviewer: Sonnet 5 lane, separate process, read-only worktree at target `5c90bb6`, base `0a09cfb`. Verdict: **FAIL**, one blocking finding.
- **Validated by:** the coordinator (Fable 5.1), each finding against source at the pinned target, 2026-09-21.

| # | Severity | Finding | Validation | Ruling |
|---|---|---|---|---|
| 1 | blocking | A `**Dead words:**` marker not at the start of a line is skipped silently; a stale override installs and the tool reports every override matches. | **Confirmed by reproduction:** a one-line local file with the marker mid-line, quoting a sentence the playbook does not carry, gives `0 dead-words item(s) checked; every override still matches` and exit 0 from `5c90bb6:scripts/check-local.sh`. The coordinator had found the same class independently by running the sibling edition's script against real local files, and the grammar was completed to fail closed (ADR 0005 decision 3, commit `6734fa1`) before this review returned. | Fix: the bare marker anywhere but the start of a line is exit 2; zero items checked in a file that has entries is never reported as "every override matches". |
| 2 | minor | "Same grammar, byte for byte" is untrue: 5 of the 44 shared vectors fail. | **Confirmed** — three are over-strict (a tab after the marker, a trailing period, trailing blanks), two are finding 1; fenced code blocks are not ignored. The vectors file reached this repository in `6734fa1`, after the target. | Fix: run every vector; the parser reads code spans; the parity matrix pins the source's published commit. |
| 3 | minor | No test that the local file is never *created*; the wording test does not pin the Dead-words clause or the `L` numbers; one mutation is killed only by accident. | **Confirmed:** `grep -c 'Dead words' tests/rulebook_test.sh` is 0 at the target; no `assert_absent` names the local file. | Fix all three. |
| 4 | minor | `ARCHITECTURE.md:58` says "approximately 8 KB"; `AGENTS.md` is 9,526 bytes. | **Confirmed.** | Fix. |
| 5 | minor | The template's examples are live entries: copied as shipped, they bind the agent. | **Confirmed** — the two example Overrides carry real Dead-words lines and pass the checker. Raised to a design point for both editions: **a template ships with no live entry**; its examples sit in a fenced block, which the checker ignores and a reader cannot mistake for an entry. | Fix here; the same ruling goes to the sibling edition. |
| I1–I7 | informational | List numbering in `INSTALL.md`; quadratic parse of one very long line; E2BIG fails closed; symlink handling; a redundant guard; untested no-damage branches; portability. | Read; I1 is fixed with the round (the list must state the real order). I2: a line-length bound is cheap and fails closed — add it. The rest need no action and are recorded here. | I1, I2 fixed; others none. |

**Outcome:** 5 of 5 findings confirmed, none dismissed. The batch stays held; the fixes get a focused mechanical re-check and then the deep review.
