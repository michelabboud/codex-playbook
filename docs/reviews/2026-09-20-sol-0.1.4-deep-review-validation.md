# Validation of Sol's deep review of 0.1.4

**Reviewed:** target `2cee956`, base `44cdfef`. **Reviewer:** Sol (`gpt-5.6-sol`, xhigh, separate
process, isolated worktree, cold-read note first). **Raw report:** `2026-09-20-sol-0.1.4-deep-review.md`
and its cold-read note, committed at `d11912c` before this validation. **Validator:** the
planning seat (Claude Fable 5.1), re-deriving each finding against the pinned commits.

**Verdict accepted: FAIL. The candidate `2cee956` is not published; the line is stopped.**
**Scoreboard: 8 findings — 8 CONFIRMED, 0 REFUTED. Four of the six blockers are in the source
rule (0.1.14) and were ported faithfully; two are this port's own.**

| # | Severity | Verdict | What I checked, and the ruling |
|---|---|---|---|
| **1** | blocking | **CONFIRMED — source defect, third correction** | Rule 3.5 says "at most two closed, unruled batches under its tip, plus the one being built", then lets batch N+2 *close* with N and N+1 still unruled — three closed, unruled batches, breaking its own invariant. The 0.1.14 rewrite fixed what the ceiling counts and left the boundary undefined. Sol's repair ("N+2 may be built but may not close") I do **not** take: a batch whose last task has landed is closed whatever we call it, and refusing to dispatch its review helps nobody. The coherent statement of the owner's choice is an **admission rule**: *a new batch starts only while at most two closed batches are unruled.* When N+2 closes there are three closed and unruled — all three reviews run — and nothing new starts until one is ruled. Exposure: three batch ranges, as already stated. |
| **2** | blocking | **CONFIRMED — this port's** | `scripts/install.sh` preflights each skill's `SKILL.md` and nothing nested, so a source tree missing `references/roster.md` installs cleanly and yields a reviews skill pointing at nothing. The plan excluded installer changes and my lane brief forbade them; that exclusion was wrong. The repository's own standard — preflight what the install depends on — applies to the roster. **Plan deviation, recorded:** the installer gains a preflight for required nested files, with a refusal test asserting zero destination changes. |
| **3** | blocking | **CONFIRMED — this port's, and I had flagged it** | The plan promised behavioural cases; the tests check four keywords and passed while finding 1 was present — which is the proof. Accepted in full: rule 3.5 gains a **normative table of worked cases** (state → what may happen next), in the source too, and the tests assert every case row. Independent semantic review stays, because no fixture proves interpretation. |
| **4** | blocking | **CONFIRMED** | The roster claims single ownership of tier definitions while the subagents skill's bullets and rule 3.1 restate who does what. The boundary, now stated: **numbered rules own assignments** (which tier does which work and which review); **the roster owns selection** (what a tier *is*), the optional second family, the binding and the evidence. The roster's "trusted with" column and its kinds-of-review table go; the test is renamed to the structural thing it checks. Same boundary applied to the source's `ROSTER.md`. |
| **5** | blocking | **CONFIRMED — source defect** | Rule 3.1's "never blocks the next task" reads as absolute; rules 3.3 and 3.5 stop the line on any blocking finding. A literal agent could continue past a mechanical blocker citing 3.1. Reworded: *a pending mechanical review never delays the next reversible task; once any review returns a blocking finding, stop-the-line applies whatever kind of review found it.* |
| **6** | blocking | **CONFIRMED — source defect** | "No programme material anywhere above it" is unsatisfiable: every path has the filesystem root above it, and a sandboxed lane cannot prove the property. Scoped to the reviewer-visible, task-managed workspace; a dedicated parent with enumerated contents where the runtime can give one; the review header records when it could not. |
| **7** | minor | **CONFIRMED** | The roster says "no product names live here" and names Codex and Claude two paragraphs later. The real policy: *canonical tier assignments carry no model product identifiers.* Narrowed in the roster, README and ADR index wording; ADR 0003 itself is not edited. |
| **8** | minor | **CONFIRMED** | The parity matrix's revision note says five rows moved to source 0.1.14, but its only `Source` field still names the earlier commit. Both provenances are now listed. |

**Sol's "could not verify", answered:**
- *Browser acceptance* — done by the coordinator before the commit, outside the target diff, so
  Sol could not see it: 1440 px and 390 px, zero horizontal overflow; Enter on the Reviews
  section shows rules 3.1–3.5; under `prefers-reduced-motion: reduce` no element animating or
  transitioning; zero console messages. Recorded here so the evidence lives in the repository.
- *Fresh-session load of the installed roster* — **still owed.** It needs the skills installed in
  a real Codex home, which replaces the owner's global Codex rules; that is his to authorize,
  not a test step. Logged in `BACKLOG.md`; the rule's own fallback (report the missing
  prerequisite; invoke the subagents skill) covers the failure case meanwhile.

**Process note.** Two independent reviews in one day, both from the other model family, both
with a cold read first, each found real defects the authoring family had read past — the second
one in text the first had already caused to be corrected. The rule under review is the rule
that required them.
