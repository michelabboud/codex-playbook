# Validation of Sol's third review (narrow) — repair round 2 and source `d992daa`

**Reviewed:** `codex-playbook` target `a440f65` (delta base `f719dfe`); `claude-code-playbook`
target `d992daa` (delta base `e5d218d`). **Reviewer:** Sol (`gpt-5.6-sol`, xhigh, separate
process, kept worktree pinned at the target, cold-read note first; it records that the target
reviews skill reached it as an operating skill before the delta — a stated limit on the cold
read, not a breach). **Raw report:** `2026-09-20-sol-0.1.4-third-review.md` and its cold-read
note, committed byte-identical at `4077b62` before this validation. **Validator:** the planning
seat (Claude Fable 5.1).

**Verdict accepted: FAIL on both, on the rule alone. Nothing is published.**
**Scoreboard: 6 findings — 6 CONFIRMED, including one that overturns my earlier dismissal.
Two blocking, both in rule 3.5 and both omitted states. The installer and the tests pass:
earlier findings 3, 4 and 5 are closed, 2 is closed with one hardening gap.**

| # | Severity | Verdict | What I checked, and the ruling |
|---|---|---|---|
| **1** | blocking | **CONFIRMED** | Rule 3.2 can close a batch before its last planned task; the rule lets several of a batch's tasks run at once; and the text says what happens to a still-running task only when the closure makes the *third* unruled batch. At any lower count the result is outside the closed batch's pinned target and cannot "start" a new batch, because its first dispatch already happened. It lands with no review owed, or waits under a rule that does not exist. I wrote row 14 for the count of three only; the state exists at every count. |
| **2** | blocking | **CONFIRMED** | The ancestry rule carries *unruled* batches, defined as closed ones. A branch cut after an open batch's first dispatch inherits nothing by the letter, so the child can admit a fresh batch while the parent continues the first — or both continue the same batch and close at incomparable targets. Row 7 covers the cut and not the open state. |
| **3** | minor | **CONFIRMED — my dismissal was wrong** | I dismissed `claude-code-playbook`'s "v0.1.15 is complete" because the file is only ever published with the version it describes. Sol's answer holds: a handoff is read by local coordinators and reviewers too, and the pinned review input said something false. It says *held in review, unpublished* until the publication close-out commit. |
| **4** | minor | **CONFIRMED** | Four public summaries say the ceiling counts "unreviewed" batches. A returned review with open findings is reviewed and still unruled; a reader could conclude that a returned review frees a slot. They say "unruled", defined in place. |
| **5** | minor | **CONFIRMED — hardening, no path from a git checkout** | A symlink at `.agents` or `.agents/skills` is an ancestor of every scan root and is never reported. Git cannot represent that alongside tracked descendants, so the pinned payload is not affected; the check costs two lines and goes in. The concurrent-mutation race Sol names is recorded as out of scope for an installer run from a quiescent checkout. |
| **6** | minor | **CONFIRMED** | `PROGRESS.md` still says "the two refusals"; the changelog says the candidate was reviewed twice. Both counts corrected. |

**The reviewer's judgment, and mine.** Sol was asked to say plainly whether rule 3.5 had become
too intricate to repair, and did: *do not add rows 17–18; restate the ceiling as one invariant —
a line carries at most three admitted, not-yet-ruled batches, the open one included.* Opening
reserves an identity; closing changes state, never the count; only a ruling brings it down. I
agree with the diagnosis: every defect since the first review sat at the seam between *closed*
batches, which the rule counted, and *open* work, which admission, branching and acceptance act
on. Counting identities from admission removes the seam, and both new findings fall out of it
without a special case. The exposure the owner chose — two in review plus the one being built —
is unchanged. **The repair is not written into any edition yet:** before the third review I
told the owner that a third failure on the rule would go back to him as a design choice rather
than become another patch, and it has.

**Process note.** Four independent reviews in one day. The defect class kept shrinking —
contradiction, off-by-one, omitted states, then two further omitted states at one seam — and
the fourth reviewer named the seam. Nothing was published after the first failed review.
