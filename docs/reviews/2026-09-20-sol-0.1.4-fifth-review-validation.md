# Validation of Sol's fifth review (narrow) — rule 3.5 completed, with a residual clause

**Reviewed:** `codex-playbook` target `79cedb4` (delta base `3019b9a`); `claude-code-playbook`
target `0daf8b5` (delta base `91cb7a0`). **Reviewer:** Sol (`gpt-5.6-sol`, xhigh, separate
process, kept worktree pinned at the target, cold-read note first). **Raw report:**
`2026-09-20-sol-0.1.4-fifth-review.md` and its cold-read note, committed byte-identical at
`01176a5` before this validation. **Validator:** the planning seat (Claude Fable 5.1).

**Verdict accepted: `codex-playbook` PASS · `claude-code-playbook` PASS WITH CHANGES. No
blocker. The line restarts; both are published, the source first.**
**Scoreboard: 2 findings — 2 CONFIRMED (one minor, one informational).**

| # | Severity | Verdict | What I checked, and the ruling |
|---|---|---|---|
| **1** | minor | **CONFIRMED — fixed before publication** | The source's `CLAUDE.md` section index ("two batches, by ancestry"), its open measurement item in `BACKLOG.md` ("the ceiling of two unreviewed batches") and its ADR index row for 0003 (the deleted "two closed batches" sentence) still described the superseded model. All three now state the invariant. Fixed in the source's close-out commit `0270322`, which is what `checkpoint/0.1.15` points at; rule text unchanged since the reviewed `0daf8b5`. |
| **2** | informational | **CONFIRMED — deferred loudly** | The wording contract greps the whole reviews skill, while the worked-case comparison uses the extracted rule 3.5; a required phrase could move elsewhere in the skill and leave the check green. The pinned target is not falsely green — every phrase is inside rule 3.5 today. Logged in `BACKLOG.md` as test hardening rather than changed after a passing review: a change to the contract is itself a change that would owe a review. |

**On the severity criterion.** The brief told the reviewer plainly that the residual clause
changes what is fair to call blocking, and asked it to say if that was a way of defining the
problem away. Its answer, recorded because the opposite answer would have been recorded too:
the criterion is legitimate — a named contradiction, an unsafe transition, a wrong row or a
deadlock stays blocking; the clause only gives genuinely unnamed work a conservative outcome,
and the named fix exception keeps a path to bring the count down at three. It found no unsafe,
contradictory or deadlocking residual state, judged all twenty rows consistent with the prose,
and would cut none of the controls.

**What this does not establish.** A passing semantic review of prose is a judgment, not a proof:
no coordinator has yet run a programme under this rule. The measurement the rule asks for — how
often a line carried three unruled batches — is still owed, in `BACKLOG.md`. So is the
fresh-session load of the installed roster in a real Codex home, which is the owner's to
authorize.

**The day, in numbers.** Six independent reviews from the other model family, each a separate
process against pinned commits with a cold read first: the plan (eleven findings), the first
candidate (eight, failed), its repair (ten, failed), the second repair (six, failed), the
restated rule (six, failed), the completed rule (two, passed). Forty-three findings; forty-two
confirmed at the time, and the one dismissal overturned by the next review. Nothing was
published between the first failed review and this pass.
