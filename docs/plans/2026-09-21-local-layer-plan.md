# Plan — the local layer (0.1.6)

- **Status:** source published 2026-09-23 as `checkpoint/0.1.6`, after Claude's `checkpoint/0.1.16`; private installation acceptance remains separate and was not performed under the two-playbook scope. Approved 2026-09-21 (the owner's word in conversation). Decision record: [`docs/adr/0005-the-local-layer.md`](../adr/0005-the-local-layer.md). Source: `claude-code-playbook` 0.1.16, ADR 0004.
- **Goal:** a user installs and updates from this repository and keeps their customizations, with no merge.
- **One batch, five tasks.** Tasks 2 and 3 touch the risk-class files (`scripts/`): deep review at task grain. The batch closes with a deep review on the Strong tier from the second model family; nothing is pushed before it is ruled.

| # | Task | Done when |
|---|---|---|
| 1 | **Rules text.** `AGENTS.md`: the local-layer paragraph (read `playbook-local.md` at session start if it exists; an entry wins; the three kinds; `L` numbers; absent = nothing customized) and the self-update sentence no longer speaks of replacing tailored rules. One pointer line in every managed `SKILL.md`. `AGENTS.md` stays under Codex's project-doc size limit; the existing size test still passes. | `tests/rulebook_test.sh` asserts the paragraph, and the pointer line in every skill on the managed inventory — driven by the inventory, not a hand list. |
| 2 | **`scripts/check-local.sh`** — POSIX `sh`. Arguments: the local file, the root holding `AGENTS.md`, the skills root. Exit 0 fresh (or no local file, with a message); 1 stale, each reported as `file:line`, the words, and where they were sought; 2 usage error, a named file that does not exist or escapes the two roots (`..`, absolute path, symlink), or a **Dead words:** line that does not parse. Fixed-string search with `-e`. Same grammar as the Claude edition, byte for byte. | `tests/check_local_test.sh`: every assertion seen to fail first and proven by mutation in a scratch copy — fresh, stale, unparsable, missing file, path escape, leading dash, regex metacharacters, two files in one item, no local file, CRLF. |
| 3 | **Installer.** `install.sh` runs the check in source preflight, against the repository's text, before `umask 077` and before any write; non-zero refuses, with no backup directory created and the destination unchanged. Neither script ever writes, moves or deletes the local file. `verify.sh` runs the new test. | New refusal tests in `tests/install_test.sh` (stale; unparsable), each asserting no backup directory and an unchanged destination; a test that a local file is byte-identical and keeps its mode and mtime after install, after `--replace-agents`, and after restore. |
| 4 | **`templates/playbook-local.md`** and **`INSTALL.md`** — first install offers the template; an update section (check, changelog cross-check, install); a migration section for tailoring made inside `AGENTS.md` or a skill; `codex-playbook-self-update` skill brought into line. | A cold reader can install, update and migrate from `INSTALL.md` alone. |
| 5 | **Propagation and close-out** — README, ARCHITECTURE, CONTRIBUTING, the parity matrix (source commit and tag of the Claude edition's 0.1.16, stated as an ordering dependency), the visual dataset if it carries `AGENTS.md`, CHANGELOG 0.1.6, PROGRESS, BACKLOG, PLAN, HANDOFF ("held" until published), `VERSION`, `docs/adr/README.md`, managed-resources inventory if a managed file was added. Older plans, reviews, ADRs and changelog entries untouched. | The repository describes itself truthfully at 0.1.6. |

**Reviews and publication:** as in the sibling plan — mechanical per task, deep on tasks 2–3 and at the batch boundary by the second family against pinned commits, coordinator validates every finding, `main` and `checkpoint/0.1.6` only after the ruling, and only after the Claude edition's `checkpoint/0.1.16` exists.
